# tonk notification personality — the same file every consumer repo carries.
# All keys are optional; username and avatar default to the tonk brand.
notify:
  failure: true
  recovery: true
  pull-requests: false
